CREATE TABLE Accounts (
    AccountID NUMBER PRIMARY KEY,
    AccountHolder VARCHAR2(50),
    AccountType VARCHAR2(20),
    Balance NUMBER(10,2)
);
INSERT INTO Accounts VALUES (101,'Ravi','Savings',20000);
INSERT INTO Accounts VALUES (102,'Priya','Current',15000);
INSERT INTO Accounts VALUES (103,'Suresh','Savings',30000);
INSERT INTO Accounts VALUES (104,'Anita','Savings',25000);

COMMIT;
CREATE OR REPLACE PROCEDURE ProcessMonthlyInterest
IS
BEGIN

    UPDATE Accounts
    SET Balance = Balance + (Balance * 0.01)
    WHERE AccountType = 'Savings';

    COMMIT;

    DBMS_OUTPUT.PUT_LINE('Monthly interest processed successfully.');

EXCEPTION

    WHEN OTHERS THEN
        ROLLBACK;
        DBMS_OUTPUT.PUT_LINE('Error: ' || SQLERRM);

END;
/